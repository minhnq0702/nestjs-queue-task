// ? Is it should be here?
export type OdooDoingTaskParams = {
  dbId: string;
  url: string;
  db: string;
  user: string;
  pass: string;
  model: string;
  func: string;
  records: string;
  args: string;
  kwargs: string;
};
